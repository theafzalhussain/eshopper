// Product Controller
// Modularized product CRUD logic

module.exports = {
    // Example: Get all products
    getAllProducts: (req, res) => {
        res.json({ message: 'Get all products endpoint' });
    },
    // Add more product CRUD functions here
};